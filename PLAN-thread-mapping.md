# Chat panel and persistent conversations plan

The authoritative implementation plan is the Roam page
`[[Codex Roam Lab/Chat panel and persistent conversations]]` in the `maskys` graph
(page UID `AWUQ6As24`).

Read that page through the configured Roam MCP or Roam CLI before designing or
implementing the chat panel, conversation storage, or Roam context and output
behavior. This file is intentionally only a pointer so the graph plan and
repository do not become competing copies.
